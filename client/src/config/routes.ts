const routes = {
    login: '/login',
    register: '/register',
    home: '/',
    editProfile: '/editProfile',
    dashboard: '/dashboard',
    pending: '/pending',
    completed: '/completed',
};

export default routes;
