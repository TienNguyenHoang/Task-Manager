export interface User {
    UserId: number;
    FullName: string;
    Email: string;
    PasswordHash: string;
}

export interface UserProfile {
    UserId: number;
    FullName: string;
    Email: string;
}
